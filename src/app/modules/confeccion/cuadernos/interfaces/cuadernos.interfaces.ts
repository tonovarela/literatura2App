export interface parametros {
    id_pde?: string
    numpartprod?: string
  }
  
 export interface CuadernoVerificado {
    sku1: string;
    verificado: boolean;
    descripcion: string;
  
  }
 export interface AudioRevision {
    error: HTMLAudioElement ,
    ok: HTMLAudioElement,
    scan:HTMLAudioElement
  }